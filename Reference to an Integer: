#include <stdio.h>

void swap(int &a, int &b) { // function takes refrence arguments
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int a = 10, b = 20;

    printf("a = %d, b = %d\n", a, b);
    swap(a, b); // calling function
    printf(" a = %d, b = %d\n", a, b);

    return 0;
}
